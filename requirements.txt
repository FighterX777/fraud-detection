# ============================================================
# AI-Powered Financial Fraud Detection System
# requirements.txt — Python 3.11+
# ============================================================

# ── Core ML ──────────────────────────────────────────────────
numpy>=2.0                  # required by shap
numpy<3.0                   # upper bound for stability on py3.11
pandas>=2.2.0               # compatible with numpy>=2
scikit-learn>=1.3.0
xgboost>=3.0.0              # 3.x native binary format (.ubj)
shap>=0.47.0,<0.52.0        # 0.52+ requires Python 3.12; 0.47–0.51 supports xgb 3.x on py3.11
scipy>=1.13.0               # compatible with numpy>=2
# imbalanced-learn removed — SMOTE was replaced by scale_pos_weight=577

# ── NLP ───────────────────────────────────────────────────────
transformers>=4.33.0
torch>=2.1.0
tokenizers>=0.13.3

# ── Computer Vision ───────────────────────────────────────────
torchvision>=0.16.0
opencv-python>=4.8.0.76
Pillow>=10.0.0

# ── Visualization ─────────────────────────────────────────────
matplotlib>=3.9.0           # 3.9+ compiled against numpy>=2
seaborn>=0.13.0             # 0.13+ supports numpy>=2
plotly>=5.16.1

# ── Web Backend ───────────────────────────────────────────────
fastapi>=0.103.0
uvicorn[standard]>=0.23.2
jinja2>=3.1.2
python-multipart>=0.0.6

# ── Utilities ─────────────────────────────────────────────────
joblib>=1.3.2
tqdm>=4.66.1
